# Single-Page HTML Resume

This project is a simple single-page resume website built using **only HTML** (no CSS, no JavaScript).

The layout and structure follow the reference provided in the assignment and use proper **semantic HTML5 tags**.

